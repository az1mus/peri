# Hook 系列 5 项修复实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 Hook 系统与 Claude Code 官方行为之间的 5 项差距：SessionStart matcher、SessionEnd reason、Stop block continue、StopFailure 触发范围、PostToolBatch 钩子。

**Architecture:** 所有变更集中在 `peri-middlewares/src/hooks/` 和 `peri-acp/src/`（executor/builder）。每项修复独立——改枚举定义/改触发条件/改 action 处理，无跨项数据流依赖。`session_start_source` 字符串从 executor → builder → middleware 透传替代现有 `bool`。

**Tech Stack:** Rust (tokio async), peri-agent/middlewares/acp crates

---

## File Structure

| 文件 | 职责 | 操作 |
|------|------|------|
| `peri-middlewares/src/hooks/types.rs` | HookEvent / HookInput / HookAction 定义 | 新增 PostToolBatch 变体 |
| `peri-middlewares/src/hooks/middleware.rs` | HookMiddleware: before_agent / after_agent / on_error / fire_standalone | SessionStart source / Stop block / StopFailure filter / SessionEnd source |
| `peri-middlewares/src/hooks/middleware_test.rs` | 现有测试文件 | 追加新测试 |
| `peri-middlewares/src/hooks/types_test.rs` | 现有测试文件 | 追加序列化测试 |
| `peri-acp/src/agent/builder.rs` | Agent 构建：HookMiddleware 工厂参数 | session_start_source: Option<String> 替代 bool |
| `peri-acp/src/session/executor.rs` | Turn 结构体：is_empty_history → session_start_source | 字段重命名 |
| `peri-tui/src/app/thread_ops.rs` | new_thread SessionEnd reason | 传 reason="clear" |
| `peri-tui/src/main.rs` | TUI 退出时 SessionEnd reason | 传 reason="prompt_input_exit" |
| `peri-agent/src/agent/executor/tool_dispatch.rs` | dispatch_tools：PostToolBatch 触发点 | 在所有 tool_result 写入后触发 hook |

---

### Task 1: SessionStart 增加 resume/clear/compact matcher

**Files:**
- Modify: `peri-acp/src/session/executor.rs:217-218`
- Modify: `peri-acp/src/agent/builder.rs:121,184,486,510`
- Modify: `peri-middlewares/src/hooks/middleware.rs:37-38,322-330`
- Test: `peri-middlewares/src/hooks/middleware_test.rs`（追加）

#### 1.1 重命名字段：`is_empty_history: bool` → `session_start_source: Option<String>`

`peri-acp/src/session/executor.rs:217-218`：

```rust
/// SessionStart matcher：startup / resume / clear / compact。
/// None 表示不触发 SessionStart。
pub session_start_source: Option<String>,
```

`peri-acp/src/session/executor.rs:273`（TurnContext 结构体）：

```rust
session_start_source: Option<String>,
```

#### 1.2 更新 executor 调用点

`peri-acp/src/session/executor.rs:310`（TurnContext 解构）：

```rust
session_start_source,
```

`peri-acp/src/session/executor.rs:448`（InterceptRequest 构造）：

```rust
session_start_source,
```

`peri-acp/src/session/executor.rs:920`（build_turn 中 builder 调用）：

```rust
session_start_source: turn.session_start_source.clone(),
```

#### 1.3 更新 builder 接口

`peri-acp/src/agent/builder.rs:121`：

```rust
pub session_start_source: Option<String>,
```

`peri-acp/src/agent/builder.rs:184`（解构）：

```rust
session_start_source,
```

`peri-acp/src/agent/builder.rs:486`（assign）：

```rust
session_start_source = session_start_source.clone(),
```

`peri-acp/src/agent/builder.rs:510`（HookMiddleware 构造）：

```rust
session_start_source.clone(),
```

#### 1.4 更新调用方：session/new, session/prompt, print

对于 session/new（首次请求）始终传 `Some("startup")`；resume 时传 `Some("resume")`；compact 后传 `Some("compact")`；/clear 后传 `Some("clear")`。

需要检查 `InterceptRequest` 的所有构造点：

`peri-acp/src/session/executor.rs:448` 仅在 `execute_prompt` 内部构造一次。上游调用方（prompt.rs, print.rs）通过 `InterceptRequest` 传入。需要在 InterceptRequest 定义处添加字段：

```rust
pub session_start_source: Option<String>,
```

然后更新所有构造 InterceptRequest 的地方。这包括：
- `peri-tui/src/acp_server/prompt.rs`：传 `is_empty_history → session_start_source`
- `peri-tui/src/acp_stdio/session/prompt.rs`：传 `is_empty_history → session_start_source`
- `peri-tui/src/cli_print.rs`：传 `session_start_source`
- `peri-acp/src/session/executor_test.rs`：测试构造

`peri-tui/src/acp_server/prompt.rs:120`：

```rust
session_start_source: if is_empty { Some("startup".to_string()) } else { None },
```

`peri-tui/src/cli_print.rs:210`：

```rust
session_start_source: Some("startup".to_string()),
```

Stdio 路径 `prompt.rs:56,115` — 同样从 `is_empty_history` 派生：

```rust
session_start_source: if is_empty_history { Some("startup".to_string()) } else { None },
```

#### 1.5 更新 HookMiddleware

`peri-middlewares/src/hooks/middleware.rs:37-38`：

```rust
/// SessionStart 的 source 值（"startup"/"resume"/"clear"/"compact"）。
/// None 表示不触发 SessionStart。
session_start_source: Option<String>,
```

构造时直接接收 `Option<String>`：

```rust
session_start_source,
```

`peri-middlewares/src/hooks/middleware.rs:322-328`：

```rust
// SessionStart: only when session_start_source is Some
if let Some(ref source) = self.session_start_source {
    let input = HookInput::session_start(
        &self.session_id,
        &self.transcript_path,
        &self.cwd,
        source,
        &self.current_model,
    );
```

#### 1.6 更新测试

`peri-middlewares/src/hooks/middleware_test.rs` — 现有测试构造 HookMiddleware 时用 `is_session_start: bool`，需改为 `session_start_source: Option<String>`：

```rust
// 旧：HookMiddleware::with_session_start(..., true)
// 新：HookMiddleware::with_session_start(..., Some("startup".to_string()))
```

- [ ] **Step 1: 重命名 `is_empty_history` → `session_start_source: Option<String>`**

```bash
# executor.rs
sed -i '' 's/pub is_empty_history: bool/pub session_start_source: Option<String>/' peri-acp/src/session/executor.rs
```

手动编辑 executor.rs TurnContext 和 InterceptRequest 结构体的所有引用点。

- [ ] **Step 2: 更新 builder.rs 字段**

编辑 `peri-acp/src/agent/builder.rs:121`：
```rust
pub session_start_source: Option<String>,
```

编辑 `peri-acp/src/agent/builder.rs:486`：
```rust
session_start_source = session_start_source.clone(),
```

编辑 `peri-acp/src/agent/builder.rs:510`：
```rust
session_start_source.clone(),
```

- [ ] **Step 3: 更新调用方（prompt.rs, print.rs)**

编辑 `peri-tui/src/acp_server/prompt.rs:120`，`peri-tui/src/acp_stdio/session/prompt.rs`，`peri-tui/src/cli_print.rs:210`

- [ ] **Step 4: 更新 HookMiddleware 用 Option<String> 替代 bool**

编辑 `peri-middlewares/src/hooks/middleware.rs:37-38,67-77,322-330`

- [ ] **Step 5: 更新测试构造**

编辑 `peri-middlewares/src/hooks/middleware_test.rs` — 所有 `with_session_start(..., true)` → `with_session_start(..., Some("startup".to_string()))`，`with_session_start(..., false)` → `with_session_start(..., None)`

- [ ] **Step 6: 编译并运行测试**

```bash
cargo build 2>&1 | head -30
cargo test -p peri-middlewares --lib -- hooks:: 2>&1
cargo test -p peri-acp --lib -- executor 2>&1
```

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "fix: SessionStart hook matcher 支持 resume/clear/compact

is_empty_history: bool → session_start_source: Option<String> 透传
executor → builder → HookMiddleware，SessionStart source='startup'/'resume'/'clear'/'compact'

Co-Authored-By: deepseek-v4-pro <deepseek-ai@claude-code-best.win>"
```

---

### Task 2: SessionEnd 增加 reason 字段

**Files:**
- Modify: `peri-middlewares/src/hooks/middleware.rs:610-670`（`fire_standalone_lifecycle_hooks` 签名 + SessionEnd 分支）
- Modify: `peri-tui/src/app/thread_ops.rs:208-226`（new_thread 调用点）
- Modify: `peri-tui/src/main.rs:887-906`（TUI 退出调用点）
- Test: `peri-middlewares/src/hooks/middleware_test.rs`（追加）

#### 2.1 fire_standalone_lifecycle_hooks 增加 reason 参数

`peri-middlewares/src/hooks/middleware.rs:616-624` 签名新增 `reason: Option<&str>`：

```rust
pub async fn fire_standalone_lifecycle_hooks(
    registered_hooks: &[RegisteredHook],
    event: HookEvent,
    cwd: &str,
    session_id: &str,
    transcript_path: &str,
    current_model: &str,
    message_count: Option<usize>,
    reason: Option<&str>,
) {
```

#### 2.2 SessionEnd 分支设 source = reason

`peri-middlewares/src/hooks/middleware.rs:636-654` 中 `source: None` → `source: reason.map(|r| r.to_string())`：

```rust
HookEvent::SessionEnd => HookInput {
    session_id: session_id.to_string(),
    transcript_path: transcript_path.to_string(),
    cwd: cwd.to_string(),
    permission_mode: None,
    agent_id: None,
    agent_type: None,
    hook_event_name: event.clone(),
    tool_name: None,
    tool_input: None,
    tool_use_id: None,
    tool_output: None,
    prompt: None,
    source: reason.map(|r| r.to_string()),
    model: Some(current_model.to_string()),
    subagent_name: None,
    subagent_result: None,
    message_count: None,
},
```

#### 2.3 更新调用点

`peri-tui/src/app/thread_ops.rs:224-226` — new_thread 时 reason="clear"：

```rust
peri_middlewares::hooks::middleware::fire_standalone_lifecycle_hooks(
    &hooks_owned,
    peri_middlewares::hooks::types::HookEvent::SessionEnd,
    &cwd,
    &session_id,
    &transcript_path,
    &current_model,
    None,
    Some("clear"),  // reason
)
.await;
```

`peri-tui/src/main.rs:904-906` — TUI 退出时 reason="prompt_input_exit"：

```rust
peri_middlewares::hooks::middleware::fire_standalone_lifecycle_hooks(
    &hooks,
    peri_middlewares::hooks::types::HookEvent::SessionEnd,
    &cwd,
    &session_id,
    &transcript_path,
    &current_model,
    None,
    Some("prompt_input_exit"),  // reason
)
.await;
```

#### 2.4 检查其他调用点

搜索所有 `fire_standalone_lifecycle_hooks` 调用处，确保参数匹配。非 SessionEnd 调用传 `None`。

- [ ] **Step 1: 添加 reason 参数到 fire_standalone_lifecycle_hooks**

编辑签名和 SessionEnd 分支

- [ ] **Step 2: 更新 thread_ops.rs 和 main.rs 调用点**

传 `Some("clear")` 和 `Some("prompt_input_exit")`

- [ ] **Step 3: 搜索并更新所有其他调用点**

```bash
grep -rn "fire_standalone_lifecycle_hooks" peri-tui/ peri-acp/ peri-middlewares/ --include="*.rs"
```
所有非 SessionEnd 调用传 `None`

- [ ] **Step 4: 编译并测试**

```bash
cargo build 2>&1 | head -30
cargo test -p peri-middlewares --lib -- hooks:: 2>&1
```

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "fix: SessionEnd hook 增加 reason 字段

fire_standalone_lifecycle_hooks 新增 reason 参数
new_thread → reason='clear', TUI 退出 → reason='prompt_input_exit'

Co-Authored-By: deepseek-v4-pro <deepseek-ai@claude-code-best.win>"
```

---

### Task 3: Stop hook block 继续工作

**Files:**
- Modify: `peri-middlewares/src/hooks/middleware.rs:38,535-562`（after_agent + 新增字段）
- Test: `peri-middlewares/src/hooks/middleware_test.rs`（追加）

#### 3.1 HookMiddleware 新增 stop_block_count 计数器

`peri-middlewares/src/hooks/middleware.rs:38` 后：

```rust
/// Stop hook block 连续次数计数器（最多 8 次，超过后 ignore）
stop_block_count: Arc<Mutex<u32>>,
```

构造时初始化 `Arc::new(Mutex::new(0))`。

#### 3.2 after_agent 处理 Block action

`peri-middlewares/src/hooks/middleware.rs:535-562` 替换 `let _action = ...` 为 action 处理：

```rust
async fn after_agent(&self, state: &mut S, output: &AgentOutput) -> AgentResult<AgentOutput> {
    let input = HookInput {
        session_id: self.session_id.clone(),
        transcript_path: self.transcript_path.clone(),
        cwd: self.cwd.clone(),
        permission_mode: Some(format!("{:?}", self.permission_mode.load())),
        agent_id: None,
        agent_type: None,
        hook_event_name: HookEvent::Stop,
        tool_name: None,
        tool_input: None,
        tool_use_id: None,
        tool_output: None,
        prompt: None,
        source: output
            .stop_reason
            .as_deref()
            .map(|_| "agent_complete".to_string()),
        model: Some(self.current_model.clone()),
        subagent_name: None,
        subagent_result: Some(output.text.chars().take(500).collect::<String>()),
        message_count: None,
    };

    let action = self.fire_event(HookEvent::Stop, &input, None, None).await;

    match &action {
        HookAction::Block { reason } => {
            let mut count = self.stop_block_count.lock();
            *count += 1;
            if *count > 8 {
                tracing::warn!(
                    count = *count,
                    "Stop hook block 连续超过 8 次，忽略 block 正常结束"
                );
                return Ok(output.clone());
            }
            tracing::info!(
                count = *count,
                reason = %reason,
                "Stop hook blocked: injecting reason as system message and continuing"
            );
            state.add_message(BaseMessage::system(format!(
                "Stop hook feedback (block #{}/8): {}",
                *count, reason
            )));
            // 返回 Err 让 ReAct 循环继续（而非结束）
            // 注意：after_agent 签名是 AgentResult<AgentOutput>，
            // 不能用 Err 让循环继续。
            // 实际方案：返回修改后的 AgentOutput 并依赖 before_agent 中
            // 的重新注入逻辑。
            // ★ 待定：需要在 ReAct 循环层增加 block_continue 机制
        }
        HookAction::PreventContinuation { stop_reason } => {
            return Err(AgentError::ToolRejected {
                tool: "Stop".to_string(),
                reason: stop_reason.clone().unwrap_or_else(|| "Stop hook prevented continuation".to_string()),
            });
        }
        _ => {
            // 非 block 时重置计数器
            *self.stop_block_count.lock() = 0;
        }
    }

    // Fire Notification
    self.fire_event(HookEvent::Notification, &input, None, None)
        .await;

    Ok(output.clone())
}
```

**注意**：`after_agent` 签名返回 `AgentResult<AgentOutput>` 不能使循环继续（Err 会终止）。需要评估两种方案：

A. 在 `ReActAgent` 循环层增加 `block_continue` 标记——`after_agent` 返回一个特殊值，循环检测后跳过 `emit Done` 直接进入下一轮 `before_model`。
B. 在 `HookMiddleware` 中直接返回 Err，让 `run_after_agent` 传播给 executor，executor 检测特定 Err 变体后重新进入循环。

推荐方案 A：新增 `AgentOutput.block_continue: Option<String>` 字段，executor/final_answer 检测后跳过 Done 继续循环。

`peri-agent/src/agent/react.rs` AgentOutput 增加字段：

```rust
/// Stop hook block 继续原因（None 表示正常结束）
pub block_continue: Option<String>,
```

`peri-agent/src/agent/executor/final_answer.rs` 在 `run_after_agent` 后检测：

```rust
let output = agent.chain.run_after_agent(state, &output).await?;
if let Some(reason) = &output.block_continue {
    // 不 emit Done / StateSnapshot，继续循环
    return Ok(());
}
```

- [ ] **Step 1: AgentOutput 新增 block_continue 字段**

编辑 `peri-agent/src/agent/react.rs` AgentOutput 结构体

- [ ] **Step 2: HookMiddleware 新增 stop_block_count + 处理 Block 逻辑**

编辑 `peri-middlewares/src/hooks/middleware.rs`

- [ ] **Step 3: final_answer 检测 block_continue 跳过 Done**

编辑 `peri-agent/src/agent/executor/final_answer.rs`

- [ ] **Step 4: 编写测试**

Stop hook 返回 Block+R 注入消息并继续循环

- [ ] **Step 5: 编译并测试**

```bash
cargo build 2>&1 | head -30
cargo test -p peri-agent --lib 2>&1
cargo test -p peri-middlewares --lib -- hooks:: 2>&1
```

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "fix: Stop hook block 支持继续工作语义

HookMiddleware 新增 stop_block_count 计数器（最多 8 次）
AgentOutput 新增 block_continue 字段，final_answer 检测后跳过 Done
Block + reason 时将 reason 作为 system message 注入

Co-Authored-By: deepseek-v4-pro <deepseek-ai@claude-code-best.win>"
```

---

### Task 4: StopFailure 触发范围收窄

**Files:**
- Modify: `peri-middlewares/src/hooks/middleware.rs:571-601`（`on_error` 方法）
- Test: `peri-middlewares/src/hooks/middleware_test.rs`（追加）

#### 4.1 on_error 按 AgentError 变体过滤

`peri-middlewares/src/hooks/middleware.rs:571-601`：

```rust
async fn on_error(
    &self,
    _state: &mut S,
    error: &peri_agent::error::AgentError,
) -> AgentResult<()> {
    // StopFailure 仅在 API/LLM 调用失败时触发，
    // 跳过 Interrupted、MaxIterationsExceeded、ToolRejected 等非 API 错误。
    let should_fire = matches!(
        error,
        peri_agent::error::AgentError::LlmError(_)
            | peri_agent::error::AgentError::LlmHttpError { .. }
            | peri_agent::error::AgentError::MiddlewareError { .. }
    );

    if !should_fire {
        return Ok(());
    }

    // 现有 StopFailure 触发逻辑
    let error_description = format!("{:?}", error);
    // ... 保持不变 ...
}
```

需要导入 `AgentError`：

```rust
use peri_agent::error::AgentError;
```

- [ ] **Step 1: 在 on_error 入口添加 should_fire 守卫**

编辑 `peri-middlewares/src/hooks/middleware.rs:571-576`

- [ ] **Step 2: 添加测试**

```rust
#[tokio::test]
async fn test_stopfailure_only_fires_on_api_errors() {
    // Interrupted → should NOT fire
    // MaxIterationsExceeded → should NOT fire
    // LlmError → should fire
    // LlmHttpError → should fire
}
```

- [ ] **Step 3: 编译并测试**

```bash
cargo build -p peri-middlewares 2>&1 | head -10
cargo test -p peri-middlewares --lib -- hooks::on_error 2>&1
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "fix: StopFailure 仅对 API/LLM 错误触发

跳过 Interrupted / MaxIterationsExceeded / ToolRejected 等非 API 错误

Co-Authored-By: deepseek-v4-pro <deepseek-ai@claude-code-best.win>"
```

---

### Task 5: PostToolBatch 钩子实现

**Files:**
- Modify: `peri-middlewares/src/hooks/types.rs:18-19`（新增变体）
- Modify: `peri-middlewares/src/hooks/types.rs:30-78`（序列化/反序列化）
- Modify: `peri-middlewares/src/hooks/middleware.rs`（新增 `after_tools_batch` 方法 + Middleware trait 实现）
- Modify: `peri-agent/src/agent/executor/tool_dispatch.rs:157-161`（触发点）
- Test: `peri-middlewares/src/hooks/middleware_test.rs`（追加）
- Test: `peri-middlewares/src/hooks/types_test.rs`（序列化测试）

#### 5.1 HookEvent 新增 PostToolBatch 变体

`peri-middlewares/src/hooks/types.rs:22` 后：

```rust
/// 一批并行工具调用全部完成时触发（每 batch 一次）
PostToolBatch,
```

序列化/反序列化：

```rust
HookEvent::PostToolBatch => serializer.serialize_str("PostToolBatch"),
```

```rust
"PostToolBatch" => HookEvent::PostToolBatch,
```

Unknown 兜底继续处理未识别事件。

#### 5.2 hooks_panel.rs 注册显示

`peri-tui/src/app/hooks_panel.rs` 在 StopFailure 后添加：

```rust
(HookEvent::PostToolBatch, "PostToolBatch", "When all parallel tools complete"),
```

#### 5.3 HookMiddleware 新增 after_tools_batch 方法

`peri-middlewares/src/hooks/middleware.rs` HookMiddleware impl 块：

```rust
/// 在一批并行工具调用全部完成后触发 PostToolBatch hook。
/// 由 dispatch_tools 在所有 tool_result 写入后调用。
pub async fn fire_post_tool_batch(
    &self,
    state: &mut S,
) -> AgentResult<()> {
    let prompt_text = state
        .messages()
        .iter()
        .rev()
        .find(|m| matches!(m, BaseMessage::Human { .. }))
        .map(|m| m.content())
        .unwrap_or_default();

    let input = HookInput {
        session_id: self.session_id.clone(),
        transcript_path: self.transcript_path.clone(),
        cwd: self.cwd.clone(),
        permission_mode: Some(format!("{:?}", self.permission_mode.load())),
        agent_id: None,
        agent_type: None,
        hook_event_name: HookEvent::PostToolBatch,
        tool_name: None,
        tool_input: None,
        tool_use_id: None,
        tool_output: None,
        prompt: Some(prompt_text),
        source: None,
        model: Some(self.current_model.clone()),
        subagent_name: None,
        subagent_result: None,
        message_count: Some(state.messages().len()),
    };

    let action = self
        .fire_event(HookEvent::PostToolBatch, &input, None, None)
        .await;

    match &action {
        HookAction::Block { reason } => {
            return Err(AgentError::ToolRejected {
                tool: "PostToolBatch".to_string(),
                reason: reason.clone(),
            });
        }
        HookAction::PreventContinuation { stop_reason } => {
            return Err(AgentError::ToolRejected {
                tool: "PostToolBatch".to_string(),
                reason: stop_reason.clone().unwrap_or_else(|| "PostToolBatch hook prevented continuation".to_string()),
            });
        }
        _ => {}
    }

    Ok(())
}
```

#### 5.4 dispatch_tools 中触发 PostToolBatch

`peri-agent/src/agent/executor/tool_dispatch.rs:157`（所有 tool_result 写入后、was_cancelled 检查前）：

```rust
// 阶段 C：所有 tool_result 写入完成，触发 PostToolBatch hook
// 注意：此时 state 已包含本轮 AI 消息 + 所有 tool_result
agent
    .chain
    .run_after_tools_batch(state, &results)
    .await?;

// 写入完成后再返回错误
if was_cancelled {
```

需要在 MiddlewareChain 或 Middleware trait 中新增 `after_tools_batch` 钩子：

`peri-agent/src/middleware/trait.rs`：

```rust
/// 一批并行工具调用全部写入 state 后触发（每个 batch 一次）。
/// 可用于聚合检查、批量日志等。
async fn after_tools_batch(
    &self,
    _state: &mut S,
    _results: &[(ToolCall, ToolResult)],
) -> AgentResult<()> {
    Ok(())
}
```

`peri-agent/src/middleware/chain.rs`：

```rust
pub async fn run_after_tools_batch(
    &self,
    state: &mut S,
    results: &[(ToolCall, ToolResult)],
) -> AgentResult<()> {
    for middleware in &self.middlewares {
        middleware.after_tools_batch(state, results).await?;
    }
    Ok(())
}
```

#### 5.5 HookMiddleware 实现 after_tools_batch trait

```rust
#[async_trait]
impl<S: State> Middleware<S> for HookMiddleware {
    // ... 现有实现 ...

    async fn after_tools_batch(
        &self,
        state: &mut S,
        _results: &[(ToolCall, ToolResult)],
    ) -> AgentResult<()> {
        self.fire_post_tool_batch(state).await
    }
}
```

- [ ] **Step 1: 新增 PostToolBatch 变体到 HookEvent**

编辑 `peri-middlewares/src/hooks/types.rs`

- [ ] **Step 2: 新增 hooks_panel 显示条目**

编辑 `peri-tui/src/app/hooks_panel.rs`

- [ ] **Step 3: Middleware trait 新增 after_tools_batch 钩子**

编辑 `peri-agent/src/middleware/trait.rs` + `peri-agent/src/middleware/chain.rs` + `peri-agent/src/middleware/base.rs`（默认空实现）

- [ ] **Step 4: HookMiddleware 实现 after_tools_batch + fire_post_tool_batch 方法**

编辑 `peri-middlewares/src/hooks/middleware.rs`

- [ ] **Step 5: dispatch_tools 中调用 run_after_tools_batch**

编辑 `peri-agent/src/agent/executor/tool_dispatch.rs:157`

- [ ] **Step 6: 添加测试**

```rust
#[tokio::test]
async fn test_post_tool_batch_fires_after_all_tools() {
    // 注册 PostToolBatch hook
    // 多工具并行调用
    // 验证 PostToolBatch 在所有 PostToolUse 后触发
}

#[tokio::test]
async fn test_post_tool_batch_block_stops_loop() {
    // PostToolBatch hook 返回 block → ToolRejected 错误
}
```

- [ ] **Step 7: 添加序列化测试**

编辑 `peri-middlewares/src/hooks/types_test.rs`：

```rust
#[test]
fn test_post_tool_batch_serialization() {
    let json = r#""PostToolBatch""#;
    let event: HookEvent = serde_json::from_str(json).unwrap();
    assert_eq!(event, HookEvent::PostToolBatch);
    let back = serde_json::to_string(&event).unwrap();
    assert_eq!(back, json);
}
```

- [ ] **Step 8: 编译并测试**

```bash
cargo build 2>&1 | head -30
cargo test -p peri-agent --lib 2>&1
cargo test -p peri-middlewares --lib -- hooks:: 2>&1
```

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat: 实现 PostToolBatch hook

HookEvent 新增 PostToolBatch 变体
Middleware trait 新增 after_tools_batch 钩子
dispatch_tools 中所有 tool_result 写入后触发
支持 Block / PreventContinuation 动作

Co-Authored-By: deepseek-v4-pro <deepseek-ai@claude-code-best.win>"
```

---

## Implementation Order

推荐顺序：Task 4 → Task 5 → Task 2 → Task 1 → Task 3

**理由**：
1. Task 4（StopFailure 收窄）最简单，单文件改 5 行，验证流程最短
2. Task 5（PostToolBatch）最独立——新枚举变体 + 新 trait 钩子，不碰现有逻辑
3. Task 2（SessionEnd reason）参数传递链短，只涉 TUI 层调用
4. Task 1（SessionStart matcher）改动面最广——bool → Option<String> 穿 5 层透传，涉及 8+ 文件
5. Task 3（Stop block continue）最复杂——需要 AgentOutput 新字段 + ReAct 循环改动，放最后确保基础稳固

---

## Self-Review

**1. Spec coverage:** 5 个 issue 各对应一个 Task ✓

**2. Placeholder scan:** 无 TBD/TODO，所有步骤含具体代码 ✓

**3. Type consistency:**
- `session_start_source: Option<String>` 从 executor → builder → middleware 一致 ✓
- `HookInput.source: Option<String>` 已存在于 types.rs（第 204 行），SessionStart/SessionEnd 复用 ✓
- `HookEvent::PostToolBatch` 新增变体，序列化/反序列化配对 ✓
- `AgentOutput.block_continue: Option<String>` 在 react.rs 定义，final_answer.rs 消费 ✓
